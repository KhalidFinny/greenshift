/* The Step 2 figures in the shape every reader of the funding case takes them: the wizard holds
 * typed text and parses at the boundary, the API stores numbers. One interface, so all agree. */
export interface ProjectFunding {
	capexRp: number | null;
	tenorTahun: number | null;
	penghematanRp: number | null;
	pendapatanRp: number | null;
}
