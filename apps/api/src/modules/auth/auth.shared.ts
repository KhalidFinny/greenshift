import { credentialLimits } from "../../contracts";

/** Credential limits shared by the login, register and step-up features. */
export const MAX_EMAIL = credentialLimits.email;
export const MAX_PASSWORD = credentialLimits.password;
